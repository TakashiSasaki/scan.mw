# Database Design Decision Matrix

> Status note:
> This document predates the Entity / Fact / Projection terminology. The legacy term "Identifier" now conceptually maps to "Marker", and "Binding" maps to "Association". Use `docs/architecture/entity-fact-projection-data-model.md` and `docs/migrations/entity-fact-projection-runtime-migration-plan.md` as the current architectural references.


This matrix registers design decisions required for the next phase of the database structure evolution, specifically handling Bluetooth legacy identifiers, observation grouping, and future generic bindings. It tracks items pending before migration Phase 7E.

See the [Database Structure](database-structure.md) document for the current production schema.

## Decision Matrix

| Decision | Current status | Options | Recommended next action | Blocks Phase 7E? | Source documents |
|---|---|---|---|---|---|
| Identifier semantic identity representation | Decided conceptually; implementation pending | raw canonicalValue as document ID vs. ad hoc string concatenation vs. hash-only key vs. JCS payload + UUIDv5 | Represent identifier semantic identity as JCS-canonical JSON. Derive `identifierKey` as UUIDv5 over the JCS UTF-8 payload using the application-wide UUIDv5 namespace. The payload must not include `ownerId`, `objectId`, or `legacyItemId`. The identifierKey must be recomputable from the JCS payload. | Yes for implementation validation, no for conceptual decision. | Deterministic UUID Policy, Ownerless Global Identifier Model, Bluetooth Global Identity Data Model |
| Identifier ownership model | Decided conceptually; implementation pending | owner-scoped identifiers vs. ownerless global identifiers vs. separate global registry plus owner-scoped claims vs. hybrid by identifier kind | Use ownerless global identifiers conceptually. `ownerId` becomes optional in a future schema phase and is not part of identity. Decide implementation path before schema changes. | Yes | Ownerless Global Identifier Model, Phase 7D.2, Phase 7D.3 |
| IdentifierRecord.ownerId optionalization | Decided conceptually; implementation impact audited | keep required `ownerId` forever vs. optional `ownerId` non-identifying vs. remove field immediately | `ownerId` becomes optional in a future schema phase and is not part of identifier identity. Select exact runtime implementation shape in Phase 7D.5. | Yes | Phase 7D.4, Ownerless Global Identifier Model |
| Identifier runtime implementation path | Partially decided conceptually; implementation pending | optional `ownerId` only vs. optional `ownerId` plus `identityScope` vs. optional `ownerId` plus `identityModelVersion` vs. global identifiers plus claims vs. separate global identifier registry | Continue using the existing `identifiers` collection. Select `identityModelVersion` for runtime interpretation metadata (instead of `identityScope`) and do not create a separate `globalIdentifiers` collection. Runtime implementation remains pending and TypeScript/rules/blueprint changes are future Phase 7D.6+ work. | Yes | Phase 7D.4, Phase 7D.5, Ownerless Global Identifier Model |
| idPurpose removal from identifier payload | Decided | Keep `idPurpose` in canonical identifier payload vs. remove it and rely on `idKind` | Canonical identifier payload excludes `idPurpose`; `idKind = "identifier"` is sufficient purpose separation for identifier identity. | No for concept; Yes only if stale docs remain | Phase 7D.5 |
| identityModelVersion | Decided conceptually; implementation pending | Introduce runtime interpretation version on `IdentifierRecord`; exclude from UUIDv5 payload | Missing or `1` = legacy/current model, `2` = ownerless/global model. This is runtime interpretation metadata and is not part of canonical identifier semantic identity. | Yes (implementation validation) | Phase 7D.5 |
| rawPayload (future v2) | Decided conceptually; implementation pending | Use optional `rawPayload?: JsonValue`; continue `rawValue`; or both | Future v2 design uses optional `rawPayload` as non-identifying source payload. `rawPayload` is excluded from UUIDv5 identity derivation. Current runtime `rawValue` references, when present, are legacy/pre-v2 and should be labeled accordingly. | Yes (mapping validation) | Phase 7D.5 |
| scheme in semantic identity | Decided | Keep `scheme` as part of identifier identity vs. treat as non-identifying metadata | Keep `scheme` in canonical semantic identity payload and UUIDv5 derivation. | No | Phase 7D.5 |
| IdentifierRecord.objectId compatibility | Legacy-only / non-authoritative | keep direct `identifiers.objectId` assumptions vs. migrate relation assumptions fully to bindings | `objectId` is not part of identifier identity; canonical relations should live in bindings. New design must not depend on direct `identifiers.objectId`; use bindings as canonical relationship state. | Yes if direct `objectId` dependence remains. | Phase 7D.4, Ownerless Global Identifier Model |
| Phase 7D.8 Additive Schema Implementation | Implemented | Additive runtime implementation | Adds `rawPayload`, `identityModelVersion`, `identitySchemaVersion`, `canonicalizationVersion` to `IdentifierRecord`. Adds pure semantic payload helper and tests. Updates `firebase-blueprint.json` additively. Does not modify Firestore rules. | Yes | Phase 7D.8 |
| Database identity ER diagram | Documentation added | Keep only full schema doc vs. add reduced key/reference ER companion | Maintain a reduced key/reference-only ER diagram as a companion to full structure docs. | No, but helps review. | Database Identity ER Diagram, Phase 7D.4 |
| Bluetooth tag identity scope | Decided | Global vs. Owner-scoped | Bluetooth tag identity follows ownerless global identifier model. `ownerId` and `legacyItemId` must not be part of Bluetooth identifier identity. | No for conceptual decision; Yes for implementation/rules validation | Phase 7D.2, Phase 7D.3, Ownerless Global Identifier Model |
| Bluetooth tag document ID strategy | Decided | Raw ID vs. UUIDv5 | Use deterministic UUIDv5 identifier key derived from JCS payload. The payload must not include ownerId, objectId, or legacyItemId. Collision checks are global. | No | Phase 7D.3, Deterministic UUID Policy |
| Bluetooth tag canonicalization | Decided | Implicit vs. Explicit `canonicalValue` | Define explicit scheme (e.g. `bluetooth-legacy-tag-id`). Applies before global JCS key generation. | No | Phase 7D.3 |
| Bluetooth object binding semantics | Decided for current migration path | `objectIdentifierBindings` vs. `identifierTargetBindings` | Use `objectIdentifierBindings` for legacy Bluetooth migration path now. Keep `identifierTargetBindings` future-only for non-object targets. | No for design decision; Yes for implementation execution | Phase 7D.7, Phase 7D.3 |
| Bluetooth RSSI handling | Decided | Object attribute vs. Observation metadata | Keep RSSI in observation metadata only. | No | Phase 7D.3 |
| Bluetooth `linkedAt` handling | Decided | Discard vs. Map to binding timestamp | Use `linkedAt` as a candidate timestamp for bindings/events. | No | Phase 7D.3 |
| `tagType` handling | Decided | Map to identifier kind vs. Preserve in legacy metadata | Preserve raw and normalized `tagType` in object legacy metadata. Do not create identifiers from `tagType` alone. | No for design decision; Yes for implementation mapping validation | Phase 7D.1 |
| Global identifier access policy | Deferred by design for current trusted-community deployment | public signed-in read vs. community scoped vs. redacted values vs. claim-mediated visibility | Current deployment assumes a single trusted community and does not implement fine-grained ACL now. Do not add ACL-specific fields now. Future ACL behavior may be derived from observations, bindings, provenance, and community membership, with exact controlled operations deferred to a future ACL introduction phase. | No for current documentation phase; Yes for future ACL implementation | Phase 7D.5 |
| ACL future-proofing | Deferred by design | Add ACL-specific fields now vs. preserve evidence and defer ACL model | Keep ACL fields deferred for now. Preserve observation/binding/event/provenance evidence so future ACL derivation remains possible without pre-committing to ACL schema fields. | No for docs task | Phase 7D.5 |
| identifierClaims | Future-only | Add now vs. defer | Do not implement or require `identifierClaims` for Phase 7D.5/7D.6 documentation and planning. | No | Phase 7D.5 |
| `observationSetId` | Design-only | None vs. Add to `identifierObservations` | Defer to a future additive schema phase. | No | Phase 7D.2 |
| `observationSets` collection | Design-only | Implement vs. Defer | Future design, not implemented yet. | No | Phase 7D.2 |
| `identifierTargetBindings` collection | Design-only | Implement vs. Defer | Future design, not implemented yet. | Yes | Phase 7D.2 |
| Wi-Fi AP / BSSID support | Not started | Include in identifiers vs. Exclude | Defer implementation; data is highly privacy-sensitive. | No | Phase 7D.2 |
| BLE beacon vs generic Bluetooth kind | Not started | Distinct kinds vs. Unified | Defer implementation of specific beacon schema. | No | Phase 7D.2 |
| Gateway/sensor/Android companion ingestion | Not started | Client ingestion vs. Backend/trusted ingestion | Defer implementation; restrict to trusted backend paths due to privacy. | No | Phase 7D.2 |
| Radio data privacy and retention | Needs-decision | Indefinite vs. Ephemeral | Requires strict security boundaries and retention policies. | No | Phase 7D.2 |
| Raw legacy snapshot preservation | Decided | normalized-only vs. preserve legacy snapshots/provenance | Preserve raw legacy Bluetooth tag entries as migration provenance snapshots. Keep full snapshots out of UUIDv5 identity payload; keep `rawPayload` minimal and non-identifying. | No for design decision; Yes for implementation wiring | Phase 7D.7, Phase 7D.3, Phase 7D.5 |
| Firestore rules impact | Transition strategy decided; implementation deferred | Restrict vs. Broaden | Use staged transition: keep owner-scoped rules until runtime is ready, then add explicit reviewed operations for `identityModelVersion: 2`/global identifiers. No broad rule opening. | Yes | Phase 7D.7, Phase 7D.2, Phase 7D.5 |
| TypeScript schema impact | Design-only | Add types vs. Defer | Will require updating `src/types.ts` once binding decisions are finalized. `ownerId` remains required until a later explicit phase. Schema changes remain future work. | Yes | Phase 7D.3 |
| Firebase blueprint impact | Design-only | Update vs. Defer | Will require updating `firebase-blueprint.json` corresponding to schema changes. Blueprint changes remain future work. | Yes | Phase 7D.3 |
| Phase 7D.6 additive runtime schema plan | Planning/audit | N/A (documentation planning row) | Complete IdentifierRecord field classification and runtime dependency audit before any schema/rules/blueprint implementation. | Yes | Phase 7D.6 additive identifier runtime schema implementation plan |

| Phase 7D.7 readiness decisions | Decided | docs-only vs. unblock additive implementation | Blockers are resolved/classified; proceed to Phase 7D.8 additive TypeScript/blueprint implementation with ownerId optionalization staged by call-site readiness. | No (for design); Yes (for implementation tasks) | Phase 7D.7 |

## Related Documentation

- [Database Structure](database-structure.md)
- [Observation Model Migration](../migrations/observation-model-migration.md)
