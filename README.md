# scan.moukaeritai.work

[![CI](https://github.com/TakashiSasaki/scan.moukaeritai.work/actions/workflows/ci.yml/badge.svg?branch=scan.moukaeritai.work)](https://github.com/TakashiSasaki/scan.moukaeritai.work/actions/workflows/ci.yml)

A web application for registering, scanning, and managing physical objects using identifiers such as QR codes, NFC tags, manual codes, and related object metadata.

*(Note: Coverage reports are generated by CI and uploaded as artifacts. A public coverage badge can be enabled after a coverage service like Codecov is set up).*

## Project Overview

The core data model consists of:
* **Objects**: The physical items being tracked.
* **Identifiers**: The physical/digital markers attached to objects (e.g., NFC, QR, barcodes).
* **Object-Identifier Bindings**: The connection mapping a physical object to a specific identifier.
* **Identifier Observations**: Records of when and where an identifier was scanned or sighted.
* **Object Events**: Important lifecycle events related to an object.
* **Object Images**: Visual records associated with the objects.
* **Firestore Security Rules**: Defines access control and data validation for the application.
* **Tests**: A mix of ordinary pure Vitest unit tests and integrated Firestore rules emulator tests.

## Current Development Status

* **Phase 7D.10 is completed.**
* Stage 1 additive identifier metadata is allowed by Firestore rules.
* New identifier creation paths emit Stage 1 metadata.
* `rawPayload` is allowed by rules as a map but is intentionally omitted by the runtime until a formal source payload policy is finalized.
* **Phase 7E migration execution remains blocked.**
* Old `items` finalization is a small, bounded cleanup separate from Phase 7E.

## Local Development

You can validate and run the project locally using the following canonical commands:

```bash
# Install dependencies
npm ci

# Start the local development server
npm run dev

# Run TypeScript linting
npm run lint

# Run pure unit tests
npm run test

# Run Firestore rules emulator tests (requires Java)
npm run test:rules

# Run unit tests and generate coverage report
npm run test:coverage

# Build the application
npm run build
```

## Testing and Validation

* **Ordinary Vitest unit tests** (`npm run test`) validate deterministic helpers, data structures, and standard logic offline.
* **Firestore rules emulator tests** (`npm run test:rules`) run directly against the local Firebase emulator to lock in the behavior of current constraints.
* **Coverage reports** (`npm run test:coverage`) are generated using Vitest (`@vitest/coverage-v8`).
* The **CI workflow** runs on pull requests and pushes to `scan.moukaeritai.work`. It is a non-deploying workflow that purely validates lint, unit tests, rules tests, build, and coverage.

## Safety Notes

* **Do not deploy** from this task or workflow.
* **Do not execute migrations** yet.
* **Do not write production Firestore data**.
* **Phase 7E remains blocked.**

## Documentation Links

* [Tests Documentation](tests/README.md)
* [Firestore Rules Tests Documentation](tests/firestore-rules/README.md)
* [Observation Model Migration Spec](docs/migrations/observation-model-migration.md)
* [Phase 7D.10 Rules Additive Fields](docs/migrations/phase-7d10-firestore-rules-stage1-additive-fields.md)
* [Deterministic UUID Architecture](docs/architecture/deterministic-uuid.md)
* [Legacy Items Finalization Plan](docs/migrations/legacy-items-finalization-plan.md)
